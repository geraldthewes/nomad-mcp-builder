name: "cleanup"
description: "Cleanup resources for a build job"

input_schema:
  type: "object"
  required: ["job_id"]
  properties:
    job_id:
      type: "string"
      description: "ID of the job to cleanup"